export type progressIndicatorLabel = {
  comma?: boolean;
  decimal?: number;
  prefix?: string;
  suffix?: string;
};

export type progressIndicator = {
  showLabel?: boolean;
  showProgressBar?: boolean;
};
